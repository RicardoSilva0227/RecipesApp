// lib/screens/liked_recipes_screen.dart
import 'package:flutter/material.dart';

class LikedRecipesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Liked Recipes',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
